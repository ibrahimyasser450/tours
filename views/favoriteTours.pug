extends base

block content
    main.main
        .user-hide= user
        if tours.length !== 0  
            .card-container
                each tour in tours
                    .card
                        .card__header
                            .card__picture
                                .card__picture-overlay &nbsp;
                                img.card__picture-img(src=`/img/tours/${tour.imageCover}`, alt=`${tour.name}`)
                            h3.heading-tertirary
                                span= tour.name

                        .card__details
                            .favorite-tour
                                h4.card__sub-heading= `${tour.difficulty} ${tour.duration}-day tour`
                                svg.heart-icon(data-tour-id=tour.id data-liked="false")
                                    use(xlink:href='/img/icons.svg#icon-heart')
                                
                            p.card__text= tour.summary
                            .card__data
                                svg.card__icon
                                    use(xlink:href='/img/icons.svg#icon-map-pin')
                                span= tour.startLocation.description
                            .card__data
                                svg.card__icon
                                    use(xlink:href='/img/icons.svg#icon-calendar')
                                span= new Date(tour.startDates[0].date).toLocaleString('en-GB', {day: 'numeric', month: 'long'})
                            .card__data
                                svg.card__icon
                                    use(xlink:href='/img/icons.svg#icon-flag')
                                span= `${tour.locations.length} stops`
                            .card__data
                                svg.card__icon
                                    use(xlink:href='/img/icons.svg#icon-user')
                                span= `${tour.maxGroupSize} people`

                        .card__footer
                            p
                                span.card__footer-value= `$${tour.price} `
                                span.card__footer-text per person
                            p.card__ratings
                                span.card__footer-value= tour.ratingsAverage
                                span.card__footer-text= ` rating (${tour.ratingsQuantity})`
                            a.btn.btn--green.btn--small(href=`/tour/${tour.slug}`) Details
        else
            h1.empty-tours You don't have favorite tours 