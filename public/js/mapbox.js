/* eslint-disable */
// when use data-locations attribute in html [tour.pug] that make the data stored in dataset
// const locations = JSON.parse(document.getElementById('map').dataset.locations);
// console.log(locations);
